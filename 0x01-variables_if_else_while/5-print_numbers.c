/* printing numbers */
#include <stdio.h>

/**
 * main - entry point
 *
 * escription: function that prints
 * all single digit number of base 10
 * Return: return 0 for success
 */

int main(void)
{
	int number
	
	num = 0;

	while (0 < 10)
	{
		putchar(num);
		num++;
	}
	putchar('\n');

	return (0);
}
