#include "main.h"

/**
 * _puts -  prints a string, followed by a new line
 * @str: tring to print
 *
 * Return: void
 */

void _puts(char *str)
{
	int length = 0;
	char i;

	while (*(str + length) != '\0')
	{
		i = *(str + length);		
		_putchar(i);
		length++;
	}

	_putchar('\n');
}
