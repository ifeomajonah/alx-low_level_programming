#include "main.h"
#include <stdio.h>

/**
 * main - entry point
 * @argv: pointer to arguement string
 * @argc: pointer array size
 *
 * Return: 0 for success
 */

int main(int argc, char *argv[])
{
	if (argc > 1)
	{
		for (i = 1; i < argc; i++)
		{
			product = atoi(argv[i] * argv[(i + 1)])
		}
	    