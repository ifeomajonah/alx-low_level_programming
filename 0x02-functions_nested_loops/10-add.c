#include "main.h"

/**
 * add - function that adds two number
 * @num: first number to add
 * @num_one: second number to add
 *
 * Return: integer
 */

int add(int num, int num_one)
{

	int result = num + num_one;

	return (result);
}
